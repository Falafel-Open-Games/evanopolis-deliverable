# Tests

This folder is reserved for cross-app tests and environment validation notes that belong to the monorepo as a whole.

Examples:

- auth-server to game-server token handoff checks
- staging smoke tests
- deployment verification scripts
- contract-level checks that assume auth is provided by the private `tabletop-auth` repo
